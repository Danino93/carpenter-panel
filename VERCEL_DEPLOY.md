# 🚀 פרסום ב-Vercel - הוראות מהירות

## ✅ מה כבר מוכן:
- ✅ הפרויקט עלה ל-GitHub
- ✅ הכל מוכן לפרסום!

## 📱 פרסום ב-Vercel:

### שלב 1: התחבר ל-Vercel
1. לך ל: https://vercel.com
2. לחץ **"Sign Up"** או **"Log In"**
3. בחר **"Continue with GitHub"**
4. אשר את ההרשאות

### שלב 2: ייבוא הפרויקט
1. לחץ על **"Add New..."** → **"Project"**
2. בחר את ה-repository: **`Danino93/carpenter-panel`**
3. לחץ **"Import"**

### שלב 3: הגדרות בנייה
Vercel יזהה אוטומטית:
- **Framework Preset:** Vite
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

**אם לא מזהה אוטומטית:**
- Framework: **Vite**
- Build Command: `npm run build`
- Output Directory: `dist`
- Install Command: `npm install`

### שלב 4: משתני סביבה (אם יש Supabase)
אם יש לך Supabase:
1. לחץ על **"Environment Variables"**
2. הוסף:
   - `VITE_SUPABASE_URL` = כתובת ה-Supabase שלך
   - `VITE_SUPABASE_ANON_KEY` = המפתח שלך

### שלב 5: פרסום
1. לחץ **"Deploy"**
2. חכה 1-2 דקות
3. האפליקציה תהיה זמינה בכתובת: `carpenter-panel.vercel.app`

## 🎯 מה קורה אחרי הפרסום:

### האפליקציה תהיה זמינה ב:
- `https://carpenter-panel.vercel.app` (או שם אחר שתבחר)
- **HTTPS אוטומטי** - מושלם ל-PWA!
- **עדכונים אוטומטיים** - כל push ל-GitHub מעדכן את האתר

## 📱 התקנה על אייפון (אחרי הפרסום):

1. פתח את Safari באייפון
2. לך לכתובת: `https://carpenter-panel.vercel.app`
3. לחץ "שתף" → "הוסף למסך הבית"
4. מוכן! 🎉

## 🔄 עדכונים עתידיים:

כל פעם שתעשה:
```bash
git add .
git commit -m "עדכון"
git push
```

Vercel יבנה ויעדכן אוטומטית!

---

**בהצלחה! 🚀**

